name: Pipeline
on: [push, pull_request]

jobs:
    tests:
        runs-on: ubuntu-latest

        steps:
            - name: Instalar o python
              uses: actions/setup-python@v6
              with:
                python-version: '3.14'
